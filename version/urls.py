from django.urls import path

#from version.apps import VersionConfig
#from version.views import VersionCreateView

#app_name = VersionConfig.name
#urlpatterns = [
    #path("version/", VersionCreateView.as_view(), name="version_create"),
    #path("version_detail/version/<int:pk>/", VersionDetailView.as_view(), name="version_detail"),
    #path("version_edit/version/<int:pk>/", VersionUpdateView.as_view(), name="version_edit"),
    #path("version_delete/version/<int:pk>/",VersionDeleteView.as_view(), name="version_delete"),

#]